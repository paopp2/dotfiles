# Changing window focus

# change window focus within space
alt - j : yabai -m window --focus south
alt - k : yabai -m window --focus north
alt - h : yabai -m window --focus west
alt - l : yabai -m window --focus east

# change window within stack
cmd - h : yabai -m window --focus stack.prev
cmd - l : yabai -m window --focus stack.next

# change focus between external displays (left and right)
rctrl - s : yabai -m display --focus west
rctrl - g : yabai -m display --focus east

# rotate layout clockwise
shift + alt - r : yabai -m space --rotate 270

# flip along y-axis
shift + alt - y : yabai -m space --mirror y-axis 


# flip along y-axis
shift + alt - x : yabai -m space --mirror x-axis 

# toggle window float
shift + alt - t : yabai -m window --toggle float --grid 4:4:1:1:2:2

# Modifying Window Size
shift + alt - m : yabai -m window --toggle zoom-fullscreen

# balance out tree of windows (resize to occupy same area)
shift + alt - e : yabai -m space --balance


# Moving windows around
shift + alt - up : yabai -m window --swap south
shift + alt - down : yabai -m window --swap north
shift + alt - left : yabai -m window --swap west
shift + alt - right : yabai -m window --swap east

# Stacking windows
shift + alt - j : yabai -m window --stack south
shift + alt - k : yabai -m window --stack north
shift + alt - h : yabai -m window --stack west
shift + alt - l : yabai -m window --stack east


# move window and split
# ctrl + alt - j : yabai -m window --warp south
# ctrl + alt - k : yabai -m window --warp north
# ctrl + alt - h : yabai -m window --warp west
# ctrl + alt - l : yabai -m window --warp east

# move window to display left and right
rctrl + alt - s : yabai -m window --display west; yabai -m display --focus west;
rctrl + alt - g : yabai -m window --display east; yabai -m display --focus east;


# move window to prev and next space
shift + alt - p : yabai -m window --space prev;
shift + alt - n : yabai -m window --space next;

# move window to space #
rctrl + alt - 1 : yabai -m window --space 1;
rctrl + alt - 2 : yabai -m window --space 2;
rctrl + alt - 3 : yabai -m window --space 3;
rctrl + alt - 4 : yabai -m window --space 4;
rctrl + alt - 5 : yabai -m window --space 5;
rctrl + alt - 6 : yabai -m window --space 6;
rctrl + alt - 7 : yabai -m window --space 7;
rctrl + alt - 8 : yabai -m window --space 8;
rctrl + alt - 9 : yabai -m window --space 9;

# start/stop yabai
ctrl + alt - r : yabai --restart-service
ctrl + alt - s : yabai --start-service
ctrl + alt - q : yabai --stop-service
